/**
 * This code was generated by v0 by Vercel.
 * @see https://v0.dev/t/RoRcjPRmTd6
 */
import { EjunkieProduct } from '@/schema'
import { AspectRatio } from '@/components/ui/aspect-ratio'
// import { Card, CardContent, CardTitle } from '@/components/ui/card'
import Image from 'next/image'
import Link from 'next/link'
// import { Badge } from './ui/badge'

type Props = {
  comic: EjunkieProduct
}

export function ComicProductCard({ comic }: Props) {
  return (
    <div className='card relative mx-auto w-full overflow-hidden transition-transform duration-300 ease-out hover:scale-105'>
      <Link href={`/comics/${comic.id}`}>
        <AspectRatio
          ratio={12 / 17}
          className='transition-all hover:brightness-[.8]'
        >
          <Image
            src={comic.images[0] ?? ''}
            alt={comic.name ?? ''}
            fill
            sizes='(max-width: 768px) 100vw, (max-width: 1200px) 50vw, 33vw'
            className='object-cover'
          />
        </AspectRatio>
        <div className='card-body pointer-events-none absolute bottom-0 flex h-40 w-full flex-col justify-end bg-gradient-to-t from-black p-4'>
          <h2 className='card-title text-slate-200'>{comic.name ?? 'N/A'}</h2>
          <div className='flex flex-wrap gap-2 capitalize'>
            {comic.tags
              .filter(tag => tag !== null)
              .toSorted((a, b) => b!.length - a!.length)
              .map(tag => (
                <div key={tag} className='badge badge-secondary font-semibold'>
                  {tag}
                </div>
              ))}
          </div>
        </div>
      </Link>
    </div>
  )
}
